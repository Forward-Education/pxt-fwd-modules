// RPM Sensor Tests
// Type: compilation
//  rpm(): number
//  isPastThreshold(threshold: number, direction: fwdEnums.OverUnder): boolean
if (fwdSensors.rpm1.isPastThreshold(fwdSensors.rpm1.rpm(), fwdEnums.OverUnder.Over)) {

}
